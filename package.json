{
  "name": "bream",
  "version": "0.3.0",
  "description": "A WordPress starter theme.",
  "bugs": "https://github.com/admturner/bream/issues",
  "license": "GPL-3.0+",
  "author": "Adam Turner (https://adamturner.org/)",
  "repository": {
    "type": "git",
    "url": "https://github.com/admturner/bream"
  },
  "scripts": {
    "clean": "rimraf assets/{css/,images/,js/}",
    "phpcs": "vendor/bin/phpcs -v --colors --extensions=php --ignore=\"*/vendor/*,*/node_modules/*\" --standard=phpcs.ruleset.xml *.php includes/ template-parts/",
    "lintjs": "eslint src/assets/js/**",
    "uglify": "mkdirp assets/js && uglifyjs src/assets/js/*.js -o assets/js/custom.js -m --source-map && uglifyjs src/assets/js/*.js -o assets/js/custom.min.js -m -c",
    "lintscss": "stylelint \"src/assets/scss/**/*.scss\"",
    "sass": "node-sass --output-style expanded src/assets/scss -o assets/css",
    "postsass": "postcss assets/css/style.css -u autoprefixer -o ./style.css && postcss assets/css/*.css --map --use autoprefixer --use cssnano --replace",
    "svgo": "mkdirp assets/images && svgo -f src/assets/images -o assets/images",
    "build:css": "npm run lintscss -s && npm run sass -s",
    "build:js": "npm run lintjs -s && npm run uglify -s",
    "build:images": "npm run svgo -s && cpx \"src/assets/images/*.{png,jpg,gif}\" assets/images/ --update",
    "test": "npm run phpcs -s && npm run lintjs -s && npm run lintscss -s",
    "test:styleguide": "eslint style-guide/js/** || true && stylelint \"style-guide/scss/**/*.scss\" || true",
    "prebuild": "npm run clean && npm run phpcs",
    "build": "npm run build:css && npm run build:js && npm run build:images"
  },
  "browserslist": [
    "> 1%",
    "ie 8-11",
    "Firefox ESR"
  ],
  "devDependencies": {
    "rimraf": "^2.6.x",
    "mkdirp": "^0.5.1",
    "cpx": "^1.5.x",
    "autoprefixer": "^8.1.x",
    "stylelint": "^9.1.x",
    "node-sass": "^4.7.x",
    "postcss-cli": "^5.0.x",
    "cssnano": "^3.10.x",
    "eslint": "^4.18.x",
    "eslint-config-wordpress": "^2.0.x",
    "uglify-js": "^3.3.x",
    "svgo": "^1.0.x"
  }
}
